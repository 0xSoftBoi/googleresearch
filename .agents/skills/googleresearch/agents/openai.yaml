interface:
  display_name: "Googleresearch"
  short_description: "Repo-specific patterns and workflows for googleresearch"
  default_prompt: "Use the googleresearch repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true